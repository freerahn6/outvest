# 인수인계 — 다른 PC에서 종목노트 운영하기

> 이 문서는 **처음 여는 문서**다. 여기서 각 상세 문서로 흩어진다.
> 작성: 2026-08-05 · 대상: 회사 PC 외의 환경(집 PC 등)에서 이 블로그를 이어서 운영하려는 경우

| 상세 문서 | 언제 보나 |
|---|---|
| [README.md](../README.md) | 프로젝트 전체 구조·SEO·Worker 배포 |
| [관리자-콘솔.md](관리자-콘솔.md) | `/admin` 사용법·환경변수·고장 대응 |
| [집필-검수-규약.md](집필-검수-규약.md) | **글을 쓰기 전에 반드시** — 검수 절차 |

---

## 1. 이 블로그가 굴러가는 구조

```
[글 작성]
  ├── (A) https://outvest.kr/admin  ← 브라우저만 있으면 됨. 세팅 0
  └── (B) src/content/posts/*.md 직접 편집 → git push
                    │
                    ▼
        [GitHub: freerahn6/outvest]  ← 콘텐츠의 단일 진실원(SSOT). 별도 DB 없음
                    │ push 감지
                    ▼
        [Cloudflare Pages] 자동 빌드 1~3분 → https://outvest.kr
                    ▲
        [GitHub Actions cron 10분]  예약 시각 지난 글 있으면 Deploy Hook 호출
```

- **정적 사이트(Astro)** 다. 서버도 DB도 없다. 글은 마크다운 파일이고, 발행은 곧 git 커밋이다.
- 시세 카드는 별도 **Cloudflare Worker**(`kis-proxy`)가 한국투자증권 API를 중계하는 구조인데, **현재 꺼져 있다**(→ 3장).

---

## 2. 집 PC 최초 세팅 (30분)

### 2-1. 레포 두 개를 클론한다

| 레포 | 클론 위치 | 안에 든 것 |
|---|---|---|
| `freerahn6/outvest` | 아무 데나 (회사 PC는 `C:\job\analytics`) | 블로그 소스 전부 |
| `freerahn6/malgnai-brain` | **`~/.claude`** (경로 고정) | 에이전트 정의 40개·knowledge 67개·전역 CLAUDE.md |

두 번째가 중요하다. **an1~an7, 감마 같은 에이전트는 블로그 레포가 아니라 `~/.claude/agents/`에 있다.**
이걸 안 가져오면 [집필-검수-규약](집필-검수-규약.md)이 "an6에게 팩트체크를 맡겨라"라고 해도 an6이 존재하지 않는다.

```bash
git clone https://github.com/freerahn6/outvest.git
cd outvest
npm install          # package-lock.json 있으므로 버전 고정 설치됨
npm run dev          # http://localhost:4321
```

`node_modules`·`dist`·`.astro`는 커밋되지 않는다. 전부 위 명령으로 재생성된다.

### 2-2. 레포에 **없는** 것 — 세 가지

| 없는 것 | 왜 없나 | 없으면 못 하는 일 | 대처 |
|---|---|---|---|
| `.dev.vars` (루트) | 시크릿이라 gitignore | **로컬에서** `/admin` 띄우기 | [`.dev.vars.example`](../.dev.vars.example) 복사해 값 채우기. **글만 쓸 거면 불필요** |
| KIS 앱키 | Cloudflare Worker secret에 보관 | Worker 재배포·로컬 시세 테스트 | [README 5장](../README.md) 참고. 지금은 시세 기능 자체가 OFF라 무관 |
| Cloudflare·GitHub 계정 접근 | 당연히 레포 밖 | 배포·환경변수 변경 | 로그인만 하면 됨 |

**핵심: 글을 쓰고 발행하는 데는 위 세 가지가 하나도 필요 없다.** clone + npm install이면 끝이다.

### 2-3. 필요한 계정 접근 체크리스트

- [ ] GitHub `freerahn6` — outvest, malgnai-brain 두 레포 push 권한
- [ ] Cloudflare 대시보드 — Pages 프로젝트 `outvest`, Worker `kis-proxy`
- [ ] `/admin` 비밀번호 (Cloudflare 환경변수 `ADMIN_PASSWORD`에 설정된 값)

---

## 3. 지금 상태 (2026-08-05 기준)

| 기능 | 상태 | 비고 |
|---|---|---|
| 사이트 | **정상** | https://outvest.kr, 커스텀 도메인 연결됨 |
| 검색엔진 색인 | **허용** | `src/config.ts`의 `published: true` |
| `/admin` 글관리 | **정상** | 위지윅 에디터(TOAST UI) |
| 예약 발행 | **정상** | Actions cron 10분 주기 |
| **시세 카드** | **꺼짐** | `config.ts`의 `quoteApi`가 `/api/quote`인데 그 엔드포인트가 없다. 코드가 `startsWith('http')`로 판정해 **카드를 아예 렌더링하지 않는다**(의도된 가드). 켜려면 Worker 배포 후 `quoteApi`를 Worker URL로 교체 |
| 글 수 | 40편대 | `src/content/posts/` |

---

## 4. 글 쓰는 세 가지 경로

### (A) 브라우저 `/admin` — 권장. 세팅 불필요

https://outvest.kr/admin → 문장 속 **"없<u>습</u>니다"의 '습'** 을 클릭 → 비밀번호.
작성·수정·예약·삭제가 되고 저장하면 GitHub에 커밋된다. 자세한 건 [관리자-콘솔.md](관리자-콘솔.md).

집 PC에서 가장 편한 방법이다. **클론조차 필요 없다.**

### (B) 마크다운 직접 편집 → push

```bash
# src/content/posts/새글-슬러그.md 생성
npm run build        # 스키마 오류·빌드 깨짐 확인
git add src/content/posts/새글-슬러그.md
git commit -m "글 발행: 제목"
git push origin main
```

파일명이 곧 URL이다(`새글-슬러그.md` → `/posts/새글-슬러그/`).

### (C) 로컬에서 `/admin` 띄우기 — `.dev.vars` 필요

```bash
npm run build
npm run preview:api      # http://localhost:8788
```

> ⚠️ **로컬 admin에서 저장해도 실제 저장소에 커밋된다.** 시험할 땐 `.dev.vars`의
> `GITHUB_BRANCH`를 임시 브랜치로 바꿔라.

---

## 5. 발행 전 필수 — 검수 절차

**[집필-검수-규약](집필-검수-규약.md)을 따른다. 요약하면:**

1. 감마가 주제에 맞는 작성자를 배정하고 검수 강도를 판정한다
2. 초안은 **`draft: true`**로 시작한다
3. 검수는 **작성자가 아닌 다른 에이전트** 최소 2명 — 팩트체커 + **an7(리스크·면책, 상시)**
4. **[치명] 미해결 상태로는 배포하지 않는다**
5. 감마가 판정 → `draft: false` → 커밋 → push

판별 질문: **이 글에 "틀리면 다음날 정정해야 할 수치·날짜"가 있는가?** 있으면 강/표준, 없으면 경량.

### 이게 형식이 아닌 이유

2026-08-05 「국고채 4%대 시대의 배당주」 집필 시 실제로 걸러진 것들:

- 초안이 **두 달 지난 시장 프레임**에 고정 — 7월 코스피 -22%, 한은 금리 인상을 반영하지 못함 (an2가 반려)
- 절세폭 "49.5%→27.5%"가 **한계세율 혼동** — 재계산하니 실제 실익은 배당 1억원 기준 84만원 (an6)
- **"명의 분산"** 표현이 증여세·금융실명법 유인으로 읽힘 (an7)
- 커버 SVG가 **다른 글 제목을 하드코딩 출력** (an7)

치명 16건·권고 30건을 거쳐 원고가 두 번 바뀌었다. 검수를 건너뛰었으면 전부 배포 후 정정이었다.

### 에이전트를 못 쓰는 환경이라면

`~/.claude`를 클론하지 못한 상태라면, 최소한 [규약 §5 체크리스트](집필-검수-규약.md)를 **직접 손으로** 훑어라. 특히 수치의 **기준일**, 인용의 **원문 취지**, **면책 문구** 세 가지가 사고의 대부분이다.

---

## 6. frontmatter 사양

```markdown
---
title: "글 제목"
description: "검색결과·공유에 뜨는 한 줄"
pubDate: 2026-08-05
tags: ["배당투자", "절세"]
category: "기초강의"          # 기초강의 | 종목 분석 | 장전 시황 — 이 3개만 허용
cover: "/images/cover-xxx.svg"
draft: false
tickers:                      # 선택. 적으면 시세 카드가 붙는다(현재 기능 OFF)
  - { code: "005930", name: "삼성전자" }
---
```

스키마 원본: [`src/content.config.ts`](../src/content.config.ts). 정의에 없는 필드를 쓰면 **빌드가 깨진다.**

### 글 상태 3가지

| 상태 | frontmatter | 사이트에서 |
|---|---|---|
| 게시 | `draft: false` + `pubDate` 과거 | 보인다 |
| 예약 | `draft: false` + `pubDate` 미래 | 시각 되면 자동 게시(최대 10분 + 빌드 1~3분) |
| 비공개 | `draft: true` | 안 나간다 |

공개 판정의 단일 출처는 `src/lib/posts.ts`의 `isLive()`다. **새 목록·피드를 만들면 반드시 이걸 써라.**

---

## 7. 정기 점검

| 주기 | 할 일 | 안 하면 |
|---|---|---|
| 1년 | **GitHub PAT 갱신** | `/admin`이 502. [관리자-콘솔 1-1](관리자-콘솔.md) |
| 60일 무커밋 시 | Actions 워크플로 재활성화 | 예약 발행이 멈춤 (GitHub 정책) |
| 필요 시 | `node scripts/gen-covers.mjs` | 커버 SVG 생성 |
| 필요 시 | `node scripts/gen-charts.mjs` | 본문 도표 SVG 생성 |
| 필요 시 | `node scripts/fetch-krx.mjs` | 종목 목록 갱신(기본 40종목 → 전 종목) |

---

## 8. 고장났을 때

증상별 대응표는 [관리자-콘솔.md 4장](관리자-콘솔.md)에 있다. 그 밖에:

| 증상 | 조치 |
|---|---|
| 빌드 실패 "Duplicate id" 경고 | `.astro` 캐시 잔재. `rm -rf .astro && npm run build` |
| frontmatter 스키마 오류 | `src/content.config.ts`와 대조. `category` 오타가 대부분 |
| push했는데 사이트가 안 바뀜 | Cloudflare Pages → Deployments에서 빌드 실패 여부 확인 |
| 로컬 시세 카드 안 뜸 | 정상이다(3장 참조) |

---

## 9. 열려 있는 일

| # | 내용 | 왜 |
|---|---|---|
| 1 | **시세 카드 활성화** | Worker 배포 후 `config.ts`의 `quoteApi`를 Worker URL로. 지금은 기능 전체가 OFF |
| 2 | **`category` enum 확장** | 현재 `기초강의/종목 분석/장전 시황` 3개뿐이라 전략·세금 계열 글이 "기초강의"로 들어간다. 규약 §3은 기초강의를 "경량 검수" 대상으로 정의하는데, 그런 글은 시황 수치가 뼈대인 강 등급이다. **분류와 검수 등급이 어긋난다** |
| 3 | **규약에 감마 집필 시 판정자 단서 추가** | §1-1(작성자는 자기 글을 검수하지 않는다)과 §4-⑤(감마가 판정)이 감마가 작성자일 때 충돌한다. "감마 집필 시 판정은 an1 또는 마스터" 같은 단서 필요 |
| 4 | `temp/` 정리 | 이전 세션의 검수 리포트 3건이 untracked로 남아 있다. gitignore 추가 / 커밋 / 삭제 중 택일 |
| 5 | `worker/wrangler.toml`의 `ALLOW_ORIGIN` | 현재 `"*"`. `https://outvest.kr`로 좁히는 게 안전 |

---

## 10. 절대 하지 말 것

- **시크릿 커밋 금지.** `.env`, `.dev.vars`, `github_pat_…`, KIS 앱키. gitignore에 있지만 `-f`로 강제 추가하지 말 것
- **발행된 글의 슬러그(파일명) 변경 금지.** 검색엔진에 색인된 링크가 끊긴다. 삭제해도 git 이력에는 남으니 되돌릴 수 있지만, 링크는 못 되돌린다
- **[치명] 지적이 남은 글 발행 금지.** 규약 §5
- **면책 문구 없는 글 발행 금지.** 규약 §1-5
- **`main`에 직접 push가 곧 배포다.** 실험은 브랜치에서
